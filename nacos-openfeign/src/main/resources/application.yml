spring:
  application:
    name: nacos-openfeign
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

server:
  port: 18085

management:
  endpoints:
    web:
      exposure:
        include: "*"